# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.HomeController.index
POST    /pushUpdate                 controllers.HomeController.handler

GET     /cardSwiped                 controllers.HomeController.swiped
GET     /paymentProcessed           controllers.HomeController.paymentProcessed
GET     /transactionCompleted       controllers.HomeController.transactionCompleted
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

#Catch all for proxying
GET     /someProxied                controllers.HomeController.someProxyExample()
GET     /*path                      controllers.HomeController.proxy(path)